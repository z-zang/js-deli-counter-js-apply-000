var katzDeli = [];

function takeANumber(katzDeliLine, name){
  line.push(name);
  
  console.log("Welcome, " + name + ". You are number " +line.length +" in line"); 

}

function nowServing(){
  line.shift(name)
}